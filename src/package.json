{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass-watch": "sass --watch scss:css",
    "postcss-watch": "postcss css/main.css -u autoprefixer cssnano -o ../docs/css/main.css --watch",
    "eleventy-watch": "npx @11ty/eleventy --input=html --output=../docs --watch --quiet",
    "server": "http-server ../docs/",
    "dev": "npm-run-all --parallel sass-watch postcss-watch eleventy-watch server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "@11ty/eleventy-navigation": "^0.3.3",
    "autoprefixer": "^10.4.7",
    "cssnano": "^5.1.7",
    "http-server": "^14.1.1",
    "npm-run-all": "^4.1.5",
    "postcss-cli": "^9.1.0",
    "sass": "^1.51.0"
  }
}
